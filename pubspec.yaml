name: trackstatus_flutter
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.1
  firebase_auth: ^5.6.2 
  cloud_firestore: ^5.6.11

  cupertino_icons: ^1.0.8
  flutter_lints: ^6.0.0
  
  go_router: ^16.0.0
  provider: ^6.1.5

  font_awesome_flutter: '>= 4.7.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  
flutter:
  uses-material-design: true
  assets:
    - assets/images/
